@Library('roboshop') _


roboshop (
        COMPONENT             : 'frontend',
        PROJECT_NAME          : "RoboShop",
        SLAVE_LABEL           : "NGINX",
        SKIP_NEXUS_UPLOAD     : false,
        APP_TYPE              : "NGINX"
)